# -------------------------------------------------------------------------- #
# Carrega as cofigurações do programa

if [ -f $CFG_FILE ]; then
	# Se existe o arquivo vamos carregar as definições presentes
	echo -n ""
fi
